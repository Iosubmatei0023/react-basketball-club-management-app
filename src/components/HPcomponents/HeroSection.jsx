import React from 'react';

const HeroSection = () => {
  return (
    <div style={{
      textAlign: 'center',
      maxWidth: '1200px',
      width: '100%'
    }}>
      <h1 style={{
        color: '#7cb0ff',
        fontSize: '3rem',
        marginBottom: '2rem',
        fontWeight: '800'
      }}>Join the Basketball Community</h1>
    </div>
  );
};

export default HeroSection;
